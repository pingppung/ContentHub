package com.example.contenthub.repository;

import com.example.contenthub.entity.NovelSite;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NovelSiteRepository extends JpaRepository<NovelSite, Long> {
}
